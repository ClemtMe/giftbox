{% extends 'Base.twig' %}

{% block title %}La Prestation {{ libelle }}{% endblock %}

{% block content %}
    <div class="max-w-md mx-auto bg-white rounded-lg shadow-md p-6 space-y-4">
        <h1 class="text-2xl font-bold text-gray-800">Prestation : {{ libelle }}</h1>
        <p class="text-gray-700"><span class="font-semibold">Description :</span> {{ description }}</p>
        <p class="text-gray-700"><span class="font-semibold">Composition :</span> {{ unite }}</p>
        <a href="{{ url }}" class="text-blue-600 hover:underline break-words">{{ url }}</a>
        <p class="text-gray-900 font-semibold">Prix : {{ tarif }}€</p>
        <img src="/{{ globals.img_dir }}/{{ img }}" alt="{{ libelle }}" class="w-full rounded-md object-cover" />
    </div>
{% endblock %}
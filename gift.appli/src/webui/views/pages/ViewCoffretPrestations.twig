{% extends 'Base.twig' %}

{% block title %}Les catégories{% endblock %}

{% block content %}
    <div class="max-w-xl mx-auto bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-xl font-semibold mb-4">
            Prestations du coffret : <span class="text-blue-600">{{ coffret.libelle }}</span>
        </h2>
        <ul class="list-disc list-inside space-y-2">
            {% for prestation in prestations %}
                <li>
                    <a href="{{ url }}?id={{ prestation.id }}"><span class="font-bold text-gray-800">{{ prestation.libelle }}:</span></a>
                    <span class="text-gray-700">{{ prestation.description }}</span>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}